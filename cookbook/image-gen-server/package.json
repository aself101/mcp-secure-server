{
  "name": "image-gen-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for unified image generation across multiple providers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "bfl-api": "^2.0.2",
    "google-genai-api": "^1.3.0",
    "ideogram-api": "^1.1.0",
    "openai-image-api": "^3.1.0",
    "stability-ai-api": "^1.0.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "vitest": "^2.0.0"
  }
}
